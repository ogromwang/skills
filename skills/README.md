# Claude Skills Collection

个人 Claude Code 技能集合，持续收集和开发各种实用的 Claude Skills。

## 什么是 Claude Skills？

Claude Skills 是一个包含 `SKILL.md` 文件的文件夹，该文件包含元数据（`name` 和 `description`）和告诉 agent 如何执行特定任务的指令。Skills 还可以包含脚本、模板和参考资料。

> 参考: [Agent Skills Specification](https://agentskills.io/specification)

## 标准结构

```
skill-name/
├── SKILL.md          # 必需：指令 + 元数据
├── scripts/          # 可选：可执行代码
├── references/       # 可选：文档资料
└── assets/           # 可选：模板、资源
```

### Progressive Disclosure

Skills 使用渐进式披露来高效管理上下文：

1. **发现（Discovery）**: 启动时，agent 只加载每个 skill 的 name 和 description
2. **激活（Activation）**: 当任务匹配 skill 的 description 时，agent 将完整的 `SKILL.md` 读入上下文
3. **执行（Execution）**: agent 遵循指令，根据需要加载引用文件或执行捆绑的代码

## 📁 Skills 目录

| Skill | 描述 | 状态 |
|-------|------|------|
| [git-merge](#git-merge) | 安全地将当前分支合并到目标分支，并自动返回原分支 | ✅ 已完成 |

---

## git-merge

安全地将当前分支合并到目标分支，并自动返回原分支。

### 技能信息

| 属性 | 值 |
|------|-----|
| **name** | `git-merge` |
| **description** | 将当前分支安全合并到目标分支，切换到目标分支执行合并操作，完成后自动返回原分支。支持处理未提交的更改。 |
| **compatibility** | 依赖 Git，需要 Bash 环境 |
| **版本** | 1.0 |

### 使用场景

在 Claude Code 中，当用户有以下需求时自动激活：

- **明确要求合并分支**：
  - "把当前分支 merge 到 main"
  - "合并我的分支到 develop"
  - "merge A to B"
  - "merge my branch into"

- **表达合并意图**：
  - "merge into main"
  - "merge to develop"
  - "merge this feature to main"
  - "combine my branch with develop"

### 工作流程

**始终使用脚本执行**，不要手动输入 git 命令：

```bash
bash skills/git-merge/scripts/safe_merge.sh <目标分支>
```

### 功能特性

| 特性 | 说明 |
|------|------|
| **安全检查** | 自动检测未提交的更改，并提示处理方式 |
| **暂存支持** | 用户可选择暂存未提交的更改，合并完成后自动恢复 |
| **分支验证** | 自动检查目标分支是否存在 |
| **自动拉取** | 切换到目标分支后自动拉取最新代码 |
| **冲突处理** | 合并失败时提示冲突信息，并自动返回原分支 |
| **自动返回** | 无论合并成功或失败，都会返回原分支 |
| **推送选项** | 合并完成后询问是否推送到远程 |

### 使用示例

```bash
# 合并到 main 分支
bash ~/Library/Application\ Support/Claude\ Code/skills/git-merge/scripts/safe_merge.sh main

# 合并到 develop 分支
bash ~/Library/Application\ Support/Claude\ Code/skills/git-merge/scripts/safe_merge.sh develop

# 合并到 feature 分支
bash ~/Library/Application\ Support/Claude\ Code/skills/git-merge/scripts/safe_merge.sh feature/login
```

### 脚本输出示例

```
→ 当前分支: feature-login
→ 目标分支: main

◆ 检查目标分支是否存在...
→ 目标分支 'main' 存在 ✓

◆ 检查工作区状态...
→ 工作区干净 ✓

◆ 切换到目标分支 'main'...
→ 已切换到 'main' ✓

◆ 拉取目标分支最新代码...
→ 已更新 'main' ✓

◆ 执行合并: 将 'feature-login' 合并到 'main'...
→ 合并成功 ✓

是否推送到远程仓库 'main'? (y/n): y
◆ 推送到远程 'main'...
→ 推送成功 ✓

◆ 返回原分支 'feature-login'...
→ 已返回 'feature-login' ✓

========================================
✅ 合并完成!
========================================
  源分支: feature-login
  目标分支: main
========================================
```

### 目录结构

```
git-merge/
├── SKILL.md              # 技能定义
└── scripts/
    └── safe_merge.sh     # 合并脚本（可执行）
```

---

## 🚀 快速开始

### 安装技能

```bash
# 安装所有 skills
cp -r ~/Downloads/claude-skills/* ~/Library/Application\ Support/Claude\ Code/skills/

# 或单独安装某个 skill
cp -r ~/Downloads/claude-skills/git-merge ~/Library/Application\ Support/Claude\ Code/skills/
```

### 验证安装

```bash
head ~/Library/Application\ Support/Claude\ Code/skills/git-merge/SKILL.md
```

## 📖 添加新 Skill

参考 [AGENTS.md](AGENTS.md) 了解完整的开发规范。

### 快速结构

```
new-skill/
├── SKILL.md              # 必需
├── scripts/              # 可选
│   └── *.sh
├── references/           # 可选
└── assets/               # 可选
```

### 核心要点

- SKILL.md 必须包含 `name` 和 `description` 的 YAML frontmatter
- `name` 使用小写字母 + 连字符
- `description` 包含触发关键词
- 脚本使用 `set -e` 和颜色输出

## 📝 更新日志

- **2026-01-09**: 初始化项目，添加 git-merge skill
